STYLE ANCHOR — this is how the author writes. When a judgment call comes up
(register, sentence length, how much to hedge, how blunt to be, when a joke is
allowed), err toward what you see here.

<!-- This is a template. Copy it to prompts/style-anchor.md (which is
     gitignored — your writing sample never leaves this machine) and paste
     2-3 paragraphs of your own writing below this line. Until you do, the
     rules in each prompt apply on their own — the pipeline works without an
     anchor, it is just less anchored. That one file is read by every step
     (cleanup, structure, suggest), so it is the only place you need it. -->

This block is a reference sample only. Never clean it, echo it, quote it back,
summarise it, or comment on it — it describes *how* to write, never *what* to
write about.
